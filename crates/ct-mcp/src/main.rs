//! `ct-mcp` — Crossthreads MCP server (FR-UI-04).
//!
//! Exposes `crossthreads.search` / `crossthreads.recall` / etc. as MCP tools
//! over stdio, delegating to the daemon. See `docs/AGENT_API.md` §7.
//!
//! Not yet implemented — scaffold only (fast-follow, P1.x).

fn main() {
    eprintln!("ct-mcp: not implemented yet (scaffold). See docs/AGENT_API.md §7.");
}
